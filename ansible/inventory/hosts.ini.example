# Terraform generates inventory/hosts.ini automatically.
# Use this file only when running against a host Terraform does not manage.

[gitea]
203.0.113.10

[gitea:vars]
ansible_user=root
ansible_ssh_private_key_file=~/.ssh/id_ed25519
ansible_python_interpreter=/usr/bin/python3
